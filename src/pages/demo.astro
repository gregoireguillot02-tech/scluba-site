---
export const prerender = false;

// /demo was the original hardcoded "Golf du Soleil" mockup. PR A replaced
// it with the data-driven /[slug] flow, and the live demo seed is at
// /caen-la-mer (renamed from scluba-test → golf-de-caen → caen-la-mer
// across migrations 0003/0007/0008). Redirect anything still pointing at
// /demo so old QR codes, pitch links, etc. land on the right page.

return new Response(null, {
  status: 301,
  headers: { Location: '/caen-la-mer' },
});
---
