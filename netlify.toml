[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Edge function paths are declared inline via `export const config = {...}`
# in netlify/edge-functions/rate-limit.ts. No [[edge_functions]] block here
# to avoid duplicate-declaration warnings.
